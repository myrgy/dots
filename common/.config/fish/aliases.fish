alias cd.. 'cd ..'
alias .. 'cd ..'
alias ... 'cd ../..'
alias .... 'cd ../../..'
alias ..... 'cd ../../../..'

alias l ls
alias ll 'ls -la'

# Colorized cat (will guess file type based on contents)
alias ccat 'pygmentize -g'
